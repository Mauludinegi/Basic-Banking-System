const request = require('supertest'),
    app = require('../utils/createServer')

describe('Product API', () => {
    it('Should create a product and display it on the page', async () => {
        
    })
})